VITE_API_URL=http://localhost:5000
# Якщо true — при недоступному бекенді сервіси повертають mock-дані з src/services/items.ts.
# У production обовʼязково false (Zero-Mocks Policy, Етап 2).
VITE_USE_MOCK_FALLBACK=false
